export type StatState = {
  vocal: number;
  dance: number;
  visual: number;
  stress: number;
  will: number;
};

export const initialStatState: StatState = {
  vocal: 0,
  dance: 0,
  visual: 0,
  stress: 0,
  will: 20,
};
